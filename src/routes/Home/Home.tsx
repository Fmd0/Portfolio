import classes from "./Home.module.scss";

const Home = () => {
    return (
        <div className={classes["container"]}>
            <p className={classes["container__header"]}>Portfolio Home Page</p>
        </div>
    )
}

export default Home;